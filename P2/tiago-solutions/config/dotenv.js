require("dotenv").config();

module.exports = {
    MY_ADDR: process.env.MY_ADDR,
    PASSWORD: process.env.PASSWORD,
    PROVIDER_URL: process.env.PROVIDER_URL,
    ADDRESSES_FILE: process.env.ADDRESSES_FILE,
}
;
